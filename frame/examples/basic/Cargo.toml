[package]
name = "pallet-example-basic"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
frame-support = { path = "../../support", default-features = false }
frame-system = { path = "../../../pallets/frame-system", default-features = false }
