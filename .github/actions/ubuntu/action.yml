---
name: ubuntu dependencies
description: installs dependencies required to compile the node and runtime on ubuntu

runs:
  using: composite
  steps:
    - name: rust compilation prerequisites (ubuntu)
      run: |
        sudo apt-get update -yqq
        sudo apt-get install -yqq --no-install-recommends \
          libclang-dev \
          clang \
          protobuf-compiler
        rustup update
        rustup target add wasm32-unknown-unknown
        rustup component add rustfmt --toolchain nightly
        rustup component add clippy rust-src
      shell: bash
    - name: Set LIBCLANG_PATH for clang-sys (bindgen/rocksdb etc.)
      run: |
        # clang-sys needs LIBCLANG_PATH pointing to a dir containing libclang.so or libclang-*.so
        if command -v llvm-config >/dev/null 2>&1; then
          echo "LIBCLANG_PATH=$(llvm-config --libdir)" >> $GITHUB_ENV
        fi
        if [ -z "${LIBCLANG_PATH:-}" ]; then
          FOUND=$(find /usr/lib -name 'libclang*.so*' -type f 2>/dev/null | head -1)
          if [ -n "$FOUND" ]; then
            echo "LIBCLANG_PATH=$(dirname "$FOUND")" >> $GITHUB_ENV
          fi
        fi
        echo "LIBCLANG_PATH=${LIBCLANG_PATH:-not set}"
      shell: bash
