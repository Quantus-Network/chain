[package]
description = "Local stub – skips substrate-wasm-builder to avoid metadata panic with optional frame-benchmarking-cli"
edition = "2021"
name = "frame-storage-access-test-runtime"
version = "0.6.0"

[features]
default = ["std"]
no_std = []
runtime-benchmarks = []
std = []

[dependencies]
codec = { workspace = true, features = ["derive"] }
sp-core = { workspace = true }
sp-runtime = { workspace = true }
sp-trie = { workspace = true }
