[package]
name = "trie-root"
version = "0.18.0"
edition = "2018"
license = "Apache-2.0"
description = "In-memory patricia trie operations (local fork with TrieHasher)."

[dependencies]
hash-db = { path = "../hash-db", default-features = false }

[features]
default = ["std"]
std = ["hash-db/std"]
