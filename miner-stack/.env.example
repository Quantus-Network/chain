# ============================================
# Validator Node Configuration
# ============================================

# Required: Your SS58 address for mining rewards
REWARDS_ADDRESS=your_ss58_address_here

# Chain name (must match chain spec)
CHAIN=dirac

# Docker image
DOCKER_IMAGE=ghcr.io/quantus/dirac-chain:latest

# Optional: Node name (visible in network)
# NODE_NAME=my-quantus-node

# Optional: Network peer limits
# IN_PEERS=256
# OUT_PEERS=256
